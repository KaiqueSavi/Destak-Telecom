---
interface Props {
	icon: string,
    text: string
}
const { icon, text } = Astro.props;

---

<div class="flex flex-row gap-2 items-center">
    <img class="h-16" src={icon} alt=""><span class="text-sm font-UniNeue text-white font-bold">{text}</span>
</div>